import tkinter

# setting up window
window = tkinter.Tk()
window.title("Mile to Km Converter")
window.minsize(width=500, height=300)

# Creating an Entry
entry = tkinter.Entry()
entry.grid(column=1, row=0)
